#include<stdio.h>
int main(){
    float height,width;
    printf("Enter the height and width of the rectangle: ");
    scanf("%f %f",&width,&height);
    printf("The perimeter of the rectangle is: - \n %f",2*(height+width));
    scnaf("Enter the perimeter")
    return 0;
}