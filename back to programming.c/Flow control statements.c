/*  #include<stdio.h>
int main()
{
    float fahr,cel;
    int lower,upper,step;
    lower=0;
    step=10;
    upper=100;
    fahr=lower;
    while(fahr<=upper){
        cel=(5.0/9.0) * (fahr-32.0); // In c compiler we cannot use different format specifiers to perform same operations.(eg:- we cannot perform addition between numbers of different format specifiers like for float(%f) and intege(%d)//
        printf("%3.0f %6.3f \n",fahr,cel);
        fahr=fahr+step;
    }
    return 0;
}*/


/*#include<stdio.h>
int main(){
    float fahr;
    for(fahr=0; fahr<=100; fahr=fahr+10){
        printf("%3.0f %6.3f \n",fahr, (5.0/9.0) * (fahr-32.0));
    }
    return 0;
}*/


#include<stdio.h>
/*float max(float, float);
float min(float, float);
int main()
{
    float max_val, min_val;
    max 67.78, min 76.87;
    max(max_val, min _val ); //call the fuction evne not know
}
max val y;
printf("Enter the maximum value");
scanf("%d",max_val);
printf("Enter the number a:-");
scanf("%f",&y);
printf("Enter the number of the program of the couse.");
int main()
{
    int i =35;
    printf()    
}
int main(){
    float 90;
    printf("Enter the unit of the measurement:- ");
    if (n<=90)  {
        printf ("Enter following entered value has entered the matrix region:-");
        scanf("%f",)
        
    }
    
}*/
void main(){
    int a ,b,c,t,y;
    printf("Enter the code the loop");
    scanf("%f %f %f",&a,&b);
    printf("Enter the number of lines:-");
    scanf("%f %f",&t,&y);
    t=(a+b+c);
    y=t;
    printf("The required the y:- ");
    scanf("The tier one is absent:-");
    printf("The ")

}
int ca(name,marks,rank){
    int *var;
    printf("Enter the name of the candiadte,marks and the rank of the candiadate: ");
    scanf("%d",&name);
    
    
}
int main(){
    int num,count,main;
    count++;
}